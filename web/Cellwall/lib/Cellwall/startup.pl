use lib  qw( /srv/web/Cellwall/lib );

use Cellwall;
use Cellwall::Web::Index;
use Cellwall::Web::Family;
use Cellwall::Web::Sequence;
use Cellwall::Web::Search;

# Create the Cellwall object
#my $cw = new Cellwall(
#	-host => 'localhost',
#	-db   => 'cellwall',
#	-base => '/data/projects/Cellwall',
#);

# Load the SQL database
#$cw->query_all();

# Set the sql object to undef
#$cw->sql(undef);

return 1;
